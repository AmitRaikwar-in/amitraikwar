import { VStack } from '@chakra-ui/react';

const ArticlesScreen = () => {
  return (
    <VStack
      minH={'100vh'}
      id="projects"
      rowGap={20}
      bg={'black'}
      paddingTop={20}
    ></VStack>
  );
};

export default ArticlesScreen;
